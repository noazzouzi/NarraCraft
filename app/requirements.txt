# L'API de l'atelier. Le pipeline lui-même n'en dépend pas :
# `pip install -r pipeline/requirements.txt` suffit pour produire un film
# au terminal, sans serveur.
fastapi>=0.115
uvicorn[standard]>=0.32
