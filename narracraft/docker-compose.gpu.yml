# GPU override — use on machines with NVIDIA drivers:
#   docker-compose -f docker-compose.yml -f docker-compose.gpu.yml up --build

services:
  backend:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
